{
  // Feature flags this service reads. The key names are the same in every
  // service and every frontend, so one flag can be searched for by name.
  //
  // An environment variable of the same name, upper-cased, overrides what is
  // here without a code change: ENABLE_HACKER_PORTAL_FEATURE=false.
  "enable_hacker_portal_feature": true
}

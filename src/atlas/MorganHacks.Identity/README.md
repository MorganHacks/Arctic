# MorganHacks.Identity

Organizers, allowlist, roles, sessions, hacker magic-link auth. Owns `identity.*`.

Milestone M2. Security-critical — see the build plan before handing this to a new contributor.

# OS
.DS_Store

# Node
node_modules/
.next/
out/
*.tsbuildinfo
.vercel

# .NET
bin/
obj/
*.user

# Env
.env
.env.local
.env*.local

# Editors
.vscode/
.idea/

# Planning material — local only, never committed.
# The design docs and build plan live on disk alongside the repo, not in it.
doc-starter/
plan/
context.md

# Agent worktrees. These are checkouts of this same repository, and committing
# one embeds a git repo inside the repo — clones then get an empty directory
# and no way to fill it.
.claude/worktrees/

# Logs from deploy/local/dev.sh.
.local-logs/

# Design mockups, not shipped.
.mockups/

# Design brief, handed to a designer and then deleted.
ADMIN-CONSOLE-BRIEF.md

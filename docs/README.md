# docs

- [Getting set up](set-up.md) — clone to a running stack, and what to do when
  that does not work
- [Backlog](backlog.md) — the gaps we already know about, checked against the
  code rather than remembered
- [runbooks/](runbooks/) — what to do when something breaks, written before the
  event rather than during it
- [architecture/](architecture/) — decision records, and how the deployments fit
  together

The design docs this project was planned from are in `doc-starter/`; the build
plan is in `plan/`. Both are local-only and gitignored, so a fresh clone will
not have them.

# forms

Schema-driven form rendering and routing.

Questions live in config; the page renders whatever the config declares. Core fields route to typed columns, everything else to the `responses` JSON column.

**The config schema itself is not yet specified** — see the build plan, it's a blocker for M3.
